<h1> Tweets Index </h1>

	<% @tweets.each do |tweet|%>
	<li>
		<b><%= tweet.title %></b> -	<%= tweet.message %>
		<%= link_to "Delete", tweet_path(tweet), method: :delete   %>
		<%= link_to "Edit", edit_tweet_path(tweet) %>
	</li>

	<% end %>
<%= link_to "New Tweet", new_tweet_path %>